# Village 1.0.0 (28) — Sharing plans and notifying friends

**Available in TestFlight for DCK Club.** Apple approved build 28 on September 13,
2026. This build supersedes build 27 for the combined requested experience.
The production database and notification service are live. The separate website
copy update is pending Cloudflare publishing authentication.

## Provenance

- Pushed source: `d74032046015d7fa43f0929fae5fb141b7f9a05f` on
  `codex/coordination-quality-pass`.
- [EAS production build](https://expo.dev/accounts/wrywonder/projects/dolorian/builds/32ae673c-77d7-4e0b-9780-0280619b8d75):
  `32ae673c-77d7-4e0b-9780-0280619b8d75`, version `1.0.0`, build `28`,
  **FINISHED** at `2026-09-13T19:02:57.073Z`, with the application archive available.
- Production EAS variables, existing Apple signing credentials, associated domain
  and bundle ID were reused. QA fixtures/environment and screenshots are excluded
  from the archive.
- [EAS submission](https://expo.dev/accounts/wrywonder/projects/dolorian/submissions/892be7bf-350a-4bf9-b258-231328d24bbe):
  `892be7bf-350a-4bf9-b258-231328d24bbe`, submitted the exact artifact above to
  Apple app `6780107858`.
- Apple build `8d039cf9-14eb-48cb-878a-f271b9d50f1f`: processing **VALID**, beta
  review **APPROVED**, external state **IN_BETA_TESTING**.
- Associated with existing external **DCK Club** group
  `03548a54-6911-4a1d-8db1-8cb4da8652af`; all eight existing testers have access.
  English testing notes are saved and automatic tester notification is enabled.
  No additional testers were invited or personal emails sent in this release.

## Production publication

The user's September 13 “Push it” authorized the production backend, website and
TestFlight publication. The earlier approval block is resolved.

The read-only deployment preview on September 13 confirmed exactly two pending
migrations, with no seed, role, or vault changes:

1. Applied `20260913000000_plan_share_links.sql` and
   `20260913010000_plan_connection_notifications.sql` to existing Supabase project
   `ymmvdepipjmqlustbdkr`. Read-only verification confirmed both ledger entries,
   new columns, privacy-revocation triggers and connection fanout. The claim RPC
   is executable only by the service role; invite creation requires authentication.
2. Deployed `connection-push` version **4**, status **ACTIVE**, with JWT verification
   enabled. An unauthenticated request returned **401**. Existing request types
   and old requests without a plan ID remain supported.
3. Cloudflare `village-invite-links` publication is still pending. Wrangler lacked
   credentials; OAuth attempts expired while the in-app browser repeatedly timed
   out. The existing Cloudflare account signed in, but direct Worker navigation
   also timed out. Chrome's Google sign-in fallback was rejected by automatic
   approval review because Google account access was not explicitly authorized.
   No Worker deployment occurred. A live GET of a synthetic `/join/` URL confirmed
   the existing generic invitation page still hands its token to the app; the
   deployed database resolves plan binding. The pending change adds plan-specific
   web copy and preserves its display hint, without changing the Apple association
   or routes. Resume with a working authenticated publishing session; do not
   rebuild or resubmit the already released iOS artifact.
4. Submitted build 28 and verified the Apple processing, review, notes and tester
   group states above. Build 27 was not submitted.

## What to test

Share a plan directly from its details. Existing connections open the plan; new
friends can connect and then view it, including after signing in. New plans shared
with connections now send an alert that opens that plan. Private plans notify
only invited friends; edits do not repeat the alert. Plan alerts can be turned off
under Your Village → Requests → notifications. Allow iOS notifications to receive
alerts. Please try both receiving a friend's plan and sharing one yourself.

## Verification record

See [plan sharing](../plan-sharing.md) for the previous link/privacy/native checks
and [plan notifications](../plan-notifications.md) for the new rules and tests.
122 Node tests, ten SQL suites on all 25 migrations, the HTTP fixture contract,
app TypeScript, Deno function checking and native creation/preference checks pass.
A synthetic notification appeared in the simulator. Opening its system card did
not complete with the automated driver, and the locked Mac blocked direct UI
verification. Verify a notification tap on a real device during release QA.
Real Expo/APNs delivery to an installed TestFlight device has not been verified.
Background retry jobs and receipt monitoring remain deferred; existing durable
notifications provide a foreground fallback when remote delivery fails.
