import type { Story, StoryMoment } from '@/types';

/** Empty until Phase 8+ when the stories feature ships. Schema present, no data. */
export const mockStories: Story[] = [];
export const mockStoryMoments: StoryMoment[] = [];
